---
import type { TOCHeadingItem } from "@/lib/blog/types"
import Icon from "@/components/base/Icon.astro"
import TOCHeadingLink from "@/components/blog/TOCHeadingLink.astro"

interface Props {
  id: string
  title: string
  isActive: boolean
  headings: TOCHeadingItem[]
}

const { id, title, isActive, headings } = Astro.props
---

<details class="toc-section" data-disclosure open={isActive}>
  <summary data-heading-link={isActive ? "top" : `${id}-top`}>
    <span>
      {title}
    </span>
    {headings.length > 0 && (
        <span data-disclosure-chevron>
          <Icon name="mingcute:down-line" aria-hidden="true" />
        </span>
      )}
  </summary>
  {headings.length > 0 && (
      <toc-section-list>
        <ul>
          {headings.map((heading) => (
            <TOCHeadingLink
              heading={heading}
              class="toc-heading-link"
              data-heading-link={heading.slug}
            />
          ))}
        </ul>
      </toc-section-list>
    )}
</details>

<style>
  .toc-section {
    padding-block: var(--space-3xs);
    border-block: 1px solid color-mix(in oklab, var(--border) 20%, transparent);
  }

  summary {
    display: flex;
    align-items: center;
    gap: var(--space-2xs);
    padding: 0.375rem var(--space-3xs);
    border-radius: var(--radius-md);
    color: color-mix(in oklab, var(--foreground) 70%, transparent);
    font-size: var(--step--1);
    font-weight: var(--font-weight-medium);
    list-style: none;
    cursor: pointer;
    transition:
      background-color var(--motion-duration) var(--motion-ease),
      color var(--motion-duration-fast) var(--motion-ease);
  }

  summary:hover {
    background: color-mix(in oklab, var(--accent) 50%, transparent);
    color: var(--accent-foreground);
  }

  summary:focus-visible {
    outline: 2px solid var(--ring);
    outline-offset: 2px;
  }

  summary > span {
    min-inline-size: 0;
    flex: 1;
  }

  toc-section-list {
    display: block;
    margin-block-start: var(--space-3xs);
    padding-inline-start: var(--space-2xs);
    border-inline-start: 2px solid
      color-mix(in oklab, var(--border) 50%, transparent);
  }

  ul {
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
    padding: 0;
    margin: 0;
    list-style: none;
  }
</style>
