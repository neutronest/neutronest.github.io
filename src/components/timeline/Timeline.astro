---
interface Props {
  class?: string
}

const { class: className } = Astro.props
---

<ul class:list={["timeline", className]}>
  <slot />
</ul>

<style>
  .timeline {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xs);
  }
</style>
